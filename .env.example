# Public market-data collection works without credentials.
# Add credentials only when private Binance endpoints are introduced later.
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Optional endpoint overrides for regional or test environments.
BINANCE_SPOT_REST_URL=https://api.binance.com
BINANCE_FUTURES_REST_URL=https://fapi.binance.com

